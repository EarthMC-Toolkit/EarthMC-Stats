import {
    Client,
    ChatInputCommandInteraction
} from "discord.js"

export default {
	name: "link",
    execute: (client: Client, interaction: ChatInputCommandInteraction) => {
        return
    }
}